# Build Resources

Place your application icons here:

- `icon.ico` — Windows icon (256x256 recommended, .ico format)
- `icon.png` — Fallback icon (512x512 PNG)

If you don't have icons, electron-builder will use a default icon.
To create an .ico from a PNG, use: https://convertico.com/
